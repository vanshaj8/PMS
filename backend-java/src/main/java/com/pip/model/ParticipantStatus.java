package com.pip.model;

public enum ParticipantStatus {
    ELIGIBLE,
    IN_PROGRESS,
    GOALS_LOCKED,
    REVIEW_IN_PROGRESS,
    CALIBRATION_PENDING,
    CALIBRATED,
    OUTCOME_RELEASED,
    ACKNOWLEDGED,
    EXCLUDED
}

